export class SubCategoryTreidi {
  id: number;
  nombre: string;
  categoryId: number;

  constructor(id: number, nombre: string, categoryId: number) {
    this.id = id;
    this.nombre = nombre;
    this.categoryId = categoryId;
  }
}
